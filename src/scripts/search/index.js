/*
    module: index js file for search functionality
    authors: Riley Mathews
    purpose: to create elements and functionality for searching the gb database
*/

const buildSearchComponent = require("./buildSearchComponent")
const addSearchEventListener = require("./searchSubmitEventListener")

buildSearchComponent()
addSearchEventListener()